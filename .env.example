# Points the React app at the Flask backend. Defaults to
# http://localhost:5555/api if not set — only needed if you're running
# the backend on a different host/port.
VITE_API_BASE_URL=http://localhost:5555/api
